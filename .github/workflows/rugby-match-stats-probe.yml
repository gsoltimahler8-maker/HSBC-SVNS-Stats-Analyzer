name: Rugby Legacy Wellington 2017 Validation

on:
  push:
    branches:
      - probe/rugby-match-stats
    paths:
      - '.github/workflows/rugby-match-stats-probe.yml'
      - 'scripts/diagnostics/rugby_legacy_wellington_2017_probe.mjs'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  wellington-legacy-validation:
    runs-on: ubuntu-latest
    timeout-minutes: 35

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Prepare isolated Node environment
        run: |
          rm -rf /tmp/rugby-legacy-wellington-2017
          mkdir -p /tmp/rugby-legacy-wellington-2017
          cp scripts/diagnostics/rugby_legacy_wellington_2017_probe.mjs /tmp/rugby-legacy-wellington-2017/probe.mjs
          cd /tmp/rugby-legacy-wellington-2017
          printf '{"private":true,"type":"module"}\n' > package.json

      - name: Reconstruct Wellington 2017 by neighboring fixture IDs
        env:
          OUT_DIR: ${{ github.workspace }}/artifacts/rugby-legacy-wellington-2017
        run: |
          cd /tmp/rugby-legacy-wellington-2017
          node probe.mjs

      - name: Show Wellington legacy validation summary
        if: always()
        run: |
          node - <<'NODE'
          const fs = require('fs');
          const p = 'artifacts/rugby-legacy-wellington-2017/summary.json';
          if (!fs.existsSync(p)) {
            console.log('summary.json not found');
            process.exit(0);
          }
          const s = JSON.parse(fs.readFileSync(p, 'utf8'));
          console.log(JSON.stringify({
            audit: s.audit,
            seed: s.seed,
            scannedCandidateCount: s.scannedCandidateCount,
            scanMinFixtureId: s.scanMinFixtureId,
            scanMaxFixtureId: s.scanMaxFixtureId,
            kindCounts: s.kindCounts,
            reconstructedFixtureCount: s.reconstructedFixtureCount,
            minMatchedFixtureId: s.minMatchedFixtureId,
            maxMatchedFixtureId: s.maxMatchedFixtureId,
            firstDateTime: s.firstDateTime,
            lastDateTime: s.lastDateTime,
            uniqueTeamCount: s.uniqueTeamCount,
            teams: s.teams,
            roundCounts: s.roundCounts,
            statsPresentCount: s.statsPresentCount,
            commentaryPresentCount: s.commentaryPresentCount,
            pointsSummaryPresentCount: s.pointsSummaryPresentCount,
            lineupPresentCount: s.lineupPresentCount,
            fixtureIds: s.fixtureIds,
            firstFixtures: s.firstFixtures,
            lastFixtures: s.lastFixtures
          }, null, 2));
          NODE

      - name: Upload Wellington validation artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: rugby-legacy-wellington-2017
          path: artifacts/rugby-legacy-wellington-2017/
          if-no-files-found: warn
          retention-days: 14
